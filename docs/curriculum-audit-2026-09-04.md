# 学习计划内容核对（2026-09-04）

## 更正来源归属

- AIInfraGuide 第 1 章没有“第4节：storage/view 的内存布局”这一小节。
- 真实依据是 4.2「数组、指针与连续内存」、4.5「智能指针与所有权」；4.7 的 TensorView 仅演示 const 成员函数。
- PyTorch框架入门 1.2「形状操作：view, reshape, permute, squeeze, unsqueeze」可用作观察 reference。MiniTensor 的 Storage/View、连续判断与 reshape 拒绝策略是本计划设计的迁移练习，不是原文完整项目。
- 第7章 AI编译器只有概览。不能伪造“第7章·TensorIR lowering”等原文小节。MLIR、TVM、FX 等在每日卡明确标为官方补充，按当天主题查阅，而非要求阅读整份文档。
- CUDA GEMM 的分块概念可以用于 CPU 实验，但 CPU cache 与 CUDA shared memory 不等同，不要求照抄 GPU 线程代码。

## 单一内容来源

- `data/daily-plan.txt`：210 条逐日 阅读定位 / 知识点 / 主任务 / 验收结果。
- `data/guide-sources.json`：本次读取的 AIInfraGuide 真实 Markdown 标题和行号；不是模型根据任务猜出来的目录。
- `scripts/verified-curriculum.mjs`：匹配真实标题；任何不存在的小节使生成失败。
- 原生成器按关键词猜测 benchmark、按周内天数轮换知识点的逻辑已移除。页面实际呈现使用明确的每日对象。

## 进度与负荷

- 保留已完成日的键，不自动清除或把此前章节标为完成。W02-D05 是用户提供的当前锚点，不按电脑日期猜测进度。
- W02-D05：naive CPU matmul 正确性 + 栈队列理论/LC232。W02-D06：Sanitizer + LC225。W02-D07：视图/拷贝对照 + LC20。
- tiled CPU matmul 移到 W03-D02；benchmark 在 W03-D05。避免同一天要求实现、优化、benchmark 全部完成。
- 每日主线约 180 分钟（含阅读/实现/验证/留存/机动）；单项算法约 60 分钟，两项约 90 分钟。两项日总计约 4.5 小时，不再声称每天 3 小时必然完成。
- 每周第7天先补未完成/难题，再做巩固。补课不等于巩固任务已完成；大幅落后时需要延后选修或重新估时，不能只移动日历就声称学完。
- 核心仍为 30 周（2026-08-31 至 2027-03-28）。tiny-gpu-compiler、完整 FlashAttention 自研、量化、完整服务以及高级 GEMM 不设为全部必达。

## 算法覆盖边界

- 核对代码随想录 README 主目录「数组」到「图论」，保留不同方法和理论编码练习，排除总结/周总结；不声称覆盖仓库所有历史文章和额外题。
- 后续 152 个学习条目从栈与队列开始；前置 33 个去重条目安排漏题核对。它们不是 185 道不同 LeetCode 题。
- 栈队列首日为理论 + LC232，之后 LC225、LC20、LC1047、LC150、LC239、LC347。
- 第19–90天后续主目录每天两项，第91–107天前置漏题核对，第108–207天完整 Hot100 每天一题，最后3天错题重做。
- 有通过记录的前置项只复跑边界，无记录则补做，不擅自认为用户此前已完成。

## 项目要求

- A 核心是 Softmax/RMSNorm/RoPE 算子组件：正确性矩阵、dispatch/fallback、PyTorch 接入、真实 Decoder 调用和可复现性能；GEMM/MLP 可复用库或选修。
- B 核心是固定 RMSNorm+MLP 子图：导入、可检查 IR、一个安全优化模式、TIR schedule、有限 shape guard、fallback、runtime 接入和端到端对照。
- 性能必须实测；未变快、未支持、未完成要如实记录。不保证超过官方库，不保证秋招录取，不将模拟说成真实 GPU/多机实践。

## 自动检查范围

核对 210 天唯一编号、每条原文引用的真实标题/行号、当前进度与算法顺序、主目录条目覆盖、100个独立 Hot100 题号、实际渲染函数读取的任务/验收、勾选与展开状态保留、四个网页入口内容一致、生产路由。不是对用户未来每个代码实现正确性的保证，也未对浏览器作视觉检查。
